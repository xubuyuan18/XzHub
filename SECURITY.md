# 安全政策

## 支持范围

XzHub 尚处于早期开发阶段。在第一个正式版本发布前，仅保证主分支接受安全修复。

## 报告漏洞

请不要通过公开 Issue 报告以下问题：

- 凭据泄露。
- 任意命令执行。
- 路径遍历。
- 未授权访问。
- 更新机制劫持。
- TLS 校验绕过。
- 可导致用户服务器或本地数据泄露的问题。

请通过 GitHub 仓库的 Private vulnerability reporting 功能提交报告。若该功能尚未启用，请先通过仓库所有者公开资料中的联系方式请求私密沟通渠道，不要在消息中直接附带可利用细节。

报告应尽量包含：

- 受影响版本或 commit。
- 操作系统版本。
- 复现步骤。
- 实际影响。
- 可行的缓解或修复建议。

## 安全设计要求

- Token、Cookie、密码和私钥不得明文写入数据库或日志。
- HTTP 客户端默认验证 TLS 证书。
- 自定义请求必须设置超时和响应体大小限制。
- 后台任务必须限制并发。
- Agent API 必须支持鉴权，并推荐使用 HTTPS。
- 自动更新包必须经过签名验证。
- 不接受通过远程服务向客户端下发任意 Shell 命令的设计。

## 密钥意外提交

发现密钥进入 Git 历史后，仅删除文件并不足够。应立即：

1. 吊销或轮换密钥。
2. 评估访问日志和影响范围。
3. 从当前分支删除敏感内容。
4. 根据风险决定是否清理 Git 历史。
5. 在不泄露密钥的前提下记录事件和修复措施。

## 负责任披露

维护者会尽力确认报告、评估风险并协调修复。由于项目由社区维护，目前不承诺固定响应时间或漏洞奖励。
